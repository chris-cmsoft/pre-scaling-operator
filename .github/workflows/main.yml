name: PreScaleOperator
on:
  push:
    branches:
      - master
jobs:
  Tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout codebase
        uses: actions/checkout@v1
      - name: Run tests
        uses: make test